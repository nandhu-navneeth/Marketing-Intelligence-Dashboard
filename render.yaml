services:
  - type: web
    name: marketing-intel-dashboard
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run app/Home.py --server.port $PORT --server.address 0.0.0.0
    autoDeploy: false
